# 1.12.2 Forge adapter

This module is the only location allowed to reference `GuiScreen`, `ScaledResolution`,
LWJGL2 `Mouse`/`Keyboard`, legacy `GlStateManager`, and the Minecraft framebuffer.

The real binding is intentionally deferred until the Kairos repository, mappings,
ForgeGradle version, and current module lifecycle are available.
